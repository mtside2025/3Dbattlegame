# 3Dbattlegame

Three.js と Vite を使った、ブラウザで遊べる 3D バトルゲームです。

## 起動方法

```bash
npm install
npm run dev
```

## ビルド

```bash
npm run build
```

## 操作方法

- `W / A / S / D` または `↑ ← ↓ →` : 移動
- `Space` : 斬撃
